# ==============================================================================
# 🤖 ENTERPRISE AGENTIC RAG — REQUIREMENTS (lean, actually-used set)
# Trimmed for reliable cloud builds. The old GCP/evals/guardrail packages were
# removed (unused after the local-first migration); git history keeps the full list.
# ==============================================================================

# --- Web / UI ---
fastapi
uvicorn[standard]
streamlit
requests
python-dotenv
numpy

# --- Agent orchestration + LLM ---
langgraph
langchain-core
langchain-groq
langchain-openai          # only used by the optional Portkey gateway seam
portkey-ai                # optional LLM gateway (activates when PORTKEY_API_KEY is set)

# --- Retrieval stack (all local/free) ---
sentence-transformers     # local embeddings (pulls torch)
qdrant-client             # vector database client
flashrank                 # local cross-encoder reranking

# --- Ingestion / document parsing ---
pypdf
python-docx
python-pptx
beautifulsoup4

# --- Observability (optional; set LOGFIRE_TOKEN to enable) ---
logfire
