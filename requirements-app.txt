# Runtime deps for the query app (Streamlit + LangGraph agent, in-process).
# Trimmed from requirements.txt — no GCP, no ingestion/office parsers, no evals.
streamlit
langgraph
langchain-core
langchain-groq
nemoguardrails               # primary input guardrail (falls back to the LLM gate if absent)
qdrant-client
sentence-transformers        # local embeddings (pulls torch)
flashrank                    # local cross-encoder reranking
python-dotenv
logfire
requests
